import styled from 'styled-components'
import {
    space,
    layout,
    typography,
    color,
    flexbox,
} from 'styled-system'

const Box = styled('div')(space, layout, typography, color, flexbox)

export default Box
